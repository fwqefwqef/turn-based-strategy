# Turn Based Strategy

Unity SRPG project (private development workspace).

## Requirements

- Unity version: see `ProjectSettings/ProjectVersion.txt`
- Open this folder as the Unity project root (the folder containing `Assets/` and `ProjectSettings/`).

## Notes

- This repo is intended to stay **private** while the project still depends on `Assets/TBS Framework`.
- Do not publish this repository as-is. See `Assets/CLEAN_RUNTIME_REWRITE_PLAN.md` for the public-release rewrite plan.
- Cursor migration handoff notes: `Assets/Docs/Rewrite/CURSOR_HANDOFF.md`
