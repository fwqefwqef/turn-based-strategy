# Terrain Effect Checks

Runs the real terrain-effect lifecycle against lightweight host adapters.

```powershell
dotnet run --project 'Assets/Tools~/TerrainEffectChecks/TerrainEffectChecks.csproj'
```

Checks cover starting bonuses, occupancy changes, temporary duration refresh and expiry, persistent Burn, and player-only Black Fog exposure.
